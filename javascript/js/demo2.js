//alert("Hello, World!"); // Display a simple alert box with a message

let name = "Alice";
const age = 20;
var city = "New York";
let isStudent = true; 
let hobbies = ["reading", "traveling", "coding"]; // Declare an array 
let person = { // Declare an object
    firstName: "John",
    lastName: "Doe",
    age: 25
};

alert("Name: " + name + "\nAge: " + age + "\nCity: " + city); // Display the values of variables in an alert box
let fruits = ["apple", "banana", "orange"]; // Declare an array of fruits

for (let i = 0; i < fruits.length; i++) { // Loop through the array
    console.log(fruits[i]); // Print each fruit to the console
}
